// Antes da refatoração
class Pedido {
  // Muitos métodos e propriedades
}

// Após a refatoração
class Pedido {
  // Propriedades do pedido
}

class PedidoItem {
  // Métodos e propriedades específicos dos itens do pedido
}