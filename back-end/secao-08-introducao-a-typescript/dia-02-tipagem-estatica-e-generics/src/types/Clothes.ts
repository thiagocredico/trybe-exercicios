// Cada roupa terá: nome, preço e tamanho.
export type Clothes = {
  name: string,
  price: number,
  size: string // '40', '42', '12', '14'
  // no banco so armazemos como numero o que iremos calcular 
}