// Isso é apenas um arquivo vazio simulado para que o Sequelize
// não quebre ao rodar `npm run db:reset` sem um seed real

export default {
  async up() {
    return true;
  },
  async down() {
    return true;
  },
};
