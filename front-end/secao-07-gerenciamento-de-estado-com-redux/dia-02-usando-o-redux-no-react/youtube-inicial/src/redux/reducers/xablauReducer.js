const INITIAL_STATE = { turma: 29, tribo: 'A' };
const xablauReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
  default:
    return state;
  }
};

export default xablauReducer;
